создать текст: строка = "Привет,";
создать тт: строка = " мир!";

создать x: число = 2;
создать y: число = 2;
создать z: булев = x > y;
создать и: булев = x > 1;

вывести(x);
вывести(y);
вывести(z);
вывести(и);
вывести(текст);

вывести(x);
вывести(текст + тт);

создать мин: функция = функция(x, y) {
    если ( x < y ) { вернуть x; } иначе { вернуть y; }
};

вывести(мин(1, 2));

создать фиб: функция = функция(x) {
	если ( x == 0 ) {
		вернуть 0;
	}

	если ( x == 1 ) {
		вернуть 1;
	}

	вернуть фиб(x - 2) + фиб(x - 1);
};

создать рез: число = фиб(6);
вывести(-(рез-1));

создать condition: булев = истина;

создать i: число = 0;
цикл (i < 10) {
    вывести(i);
    i = i + 1;
}