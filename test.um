создать текст: строка = "Привет,";
создать тт: строка = " мир!";

создать x: число = 2;
создать y: число = 2;
создать z: булев = x > y;
создать и: булев = x > 1;

вывести(x);
вывести(y);
вывести(z);
вывести(и);
вывести(текст);

вывести(x);
вывести(текст + тт);

создать мин: функция = функция(x, y) {
    если ( x < y ) {
        вернуть x;
    } иначе {
        вернуть y;
    }
};

вывести(мин(1, 2));

создать фиб: функция = функция(x) {
	если ( x == 0 ) {
		вернуть 0;
	}

	если ( x == 1 ) {
		вернуть 1;
	}

	вернуть фиб(x - 2) + фиб(x - 1);
};

создать рез: число = фиб(6);
вывести(-(рез-1));

создать condition: булев = истина;

создать ф: число = последний([1, 2, 3, 4, 5]);
ф = первый([1, 2, 3, 4, 5]);
вывести(длина([1, 2, 3, 4, 5]));

создать мас: массив = [1, 2, 3];
вывести(мас);
добавить(мас, 4);
добавить(мас, 5);
добавить(мас, 6);
вывести(мас);

создать i: число = 0;
создать j: число = 0;
цикл (i != 10) {
    цикл (j != 10) {
        вывести(i, j);
        j = j + 1;
    }
    i = i + 1;
    j = 0;
}

i = 0;
цикл (i < длина(мас)) {
    вывести(мас[i]);
    i = i + 1;
}